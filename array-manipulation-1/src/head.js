/* exported head */
/*

PSEUDOCODE:
Define a function, named `head`, that accepts one argument, `array`.
Return the value of the first item of the `array` array.

*/
/* CODE IMPLEMENTATION:
*/

function head(array) {
  return array[0];
}
