/* exported last */
/*

PSEUDOCODE:

Define a function, named `last`, that accepts on argument, `array`.
Return the value of the last item in the `array` array.

*/

/* CODE IMPLEMENTATION:
 */

function last(array) {
  return array[array.length - 1];
}
