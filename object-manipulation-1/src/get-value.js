/* exported getValue */

/* PSEUDOCODE:

Define a function, named `getValue`, that accepts two arguments: `object` and `key`.
  Return the value of the `object` variable at index `key`.
*/

/* CODE IMPLEMENTATION:
*/

function getValue(object, key) {
  return object[key];
}
